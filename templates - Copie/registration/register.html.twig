{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}

    <h1>Inscription</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.nomuser, {
            label: 'Nom :'
        }) }}
          {{ form_row(registrationForm.prenomuser, {
            label: 'Prénom :'
        }) }}
           {{ form_row(registrationForm.datenaissuser, {
            label: 'Date de naissance :'
        }) }}
            {{ form_row(registrationForm.validationrgpd, {
            label: 'Validation de la RGPD :'
        }) }}
        {{ form_row(registrationForm.mailuser, {
            label: 'Adresse Email :'
        }) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Mot de passe :'
        }) }}
        {{ form_row(registrationForm.agreeTerms) }}
        <button type="submit" class="btn">Valider</button>
    {{ form_end(registrationForm) }}
{% endblock %}
